public enum StatField {

    // CORE IDENTIFIER

    // Frame stats
    FRAME_WIN,
    FRAME_LOSS,
    FRAME_BREAK_DISH,
    FRAME_TOTAL,

    // Match stats
    MATCH_WIN,
    MATCH_LOSS,
    MATCH_DRAW,
    MATCH_TOTAL
}